image: maven:3.8.6-openjdk-18
stages:
  - build
  - tests

backend-build:
  stage: build
  script:
    - echo "BUILD STAGE"
    - mvn clean compile
    - mvn package

backend-test:
  stage: tests
  script:
    - echo "START TEST"
    - mvn test
