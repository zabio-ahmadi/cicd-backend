stages:
  - build
  - tests

backend-build:
  stage: build
  script:
    - echo "BUILD STAGE"
    - mvn compile
    - mvn package

backend-test:
  stage: tests
  script:
    - echo "START TEST"
    - mvn test
